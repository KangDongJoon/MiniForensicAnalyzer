<Window x:Class="MiniForensicAnalyzer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MiniForensicAnalyzer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">

            <Button x:Name="OpenFileButton"
                    Width="120"
                    Height="35"
                    Click="OpenFile_Click">
                파일 열기
            </Button>

            <Button x:Name="HashButton"
                    Width="120"
                    Height="35"
                    Margin="10,0,0,0"
                    Click="HashButton_Click">
                SHA256 계산
            </Button>

            <Button x:Name="StringButton"
                    Width="120"
                    Height="35"
                    Margin="10,0,0,0"
                    Click="StringButton_Click">
                문자열 추출
            </Button>
            
        </StackPanel>

        <Border Grid.Row="1"
                BorderBrush="LightGray"
                BorderThickness="1"
                Padding="10"
                Margin="0,10,0,10">

            <StackPanel>

                <TextBlock Text="{Binding FilePath, StringFormat=파일 경로: {0}}"
                           FontWeight="Bold"
                           Margin="0,5,0,10"
                           TextWrapping="Wrap"/>

                <TextBlock Text="파일 정보: "
                   FontWeight="Bold"/>

                <TextBlock Text="{Binding FileInfoText}"
                   Margin="0,5,0,0"
                   TextWrapping="Wrap"/>
                
            </StackPanel>
            
        </Border>

        <Border Grid.Row="2"
                BorderBrush="LightGray"
                BorderThickness="1"
                Padding="10"
                Margin="0,10,0,10">
            

            <TextBlock Text="{Binding HashText, StringFormat=SHA256: {0}}"
                        FontWeight="Bold"
                        TextWrapping="Wrap"/>
        </Border>
        
        <TabControl Grid.Row="3">
            
            <TabItem Header="Hex">
                <TextBox Text="{Binding HexText}"
                 FontFamily="Consolas"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"/>
            </TabItem>

            <TabItem Header="Strings">
                <TextBox Text="{Binding StringText}"
                 FontFamily="Consolas"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"/>
            </TabItem>
            
        </TabControl>
    </Grid>
</Window>
